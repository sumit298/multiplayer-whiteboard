# Custom Authentication Configuration
# Choose one of the following authentication methods:

# Option 1: Simple token-based authentication
CUSTOM_AUTH_TOKEN=your_secret_token_here

# Option 2: JWT-based authentication (uncomment if using JWT)
# JWT_SECRET=your_jwt_secret_here

# Option 3: Room-specific tokens (handled in code)
# No environment variables needed for this approach

# Server Configuration
PORT=5959

# Note: VideoSDK-specific variables are no longer needed:
# - INFRA_TOKEN (removed)
# - API_SERVER_URL (removed)
# - BASIC_INFRA_TOKEN (removed)
